let video = document.querySelectorAll('.video');
let contend=document.querySelectorAll('.video_contant');
let list=document.querySelectorAll('.list');
import full_list from "../../bd/courses.json" assert {type:"json"};
let list_all=full_list;
array.forEach(list_all => {
    list = document.createElement('li');
    const item=document.createElement('p');
    

});